import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'activities',
  title: 'Activities',
  version: '0.1.0',
  description: 'Universal activity journal — tasks, emails, meetings, calls, notes.',
  author: 'OpenMercato',
  license: 'MIT',
}
